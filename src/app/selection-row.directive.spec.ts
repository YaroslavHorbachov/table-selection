import { SelectionRowDirective } from './selection-row.directive';

describe('SelectionRowDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectionRowDirective();
    expect(directive).toBeTruthy();
  });
});
